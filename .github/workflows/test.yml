Repo:
"deployment": "deployment"

"config": "name: Deploy Connector Templates

on:
  push:
    branches:
      - main
    paths:
      - 'auto-generated/integrations/terraform-modules/prod/values.yaml'
